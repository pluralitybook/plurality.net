# dependencies (bun install)
node_modules

# output
out
dist
*.tgz
!worker/vendor/*.tgz
# Vendored gateway tarball (must be committed for clone/CI)

# code coverage
coverage
*.lcov

# test artifacts
/playwright-report
/test-results
/.lycheecache

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# Cloudflare Worker secrets (plurality-ask)
worker/.dev.vars

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# vectorize-sync-book temp ndjson batches
.vectorize-sync-*

# Claude Code scratch worktrees (never commit)
.claude/worktrees
